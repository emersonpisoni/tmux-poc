{
  "name": "tmux-poc-backend",
  "version": "1.0.0",
  "description": "Native Node backend (no dependencies) for the tmux POC",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js"
  }
}
